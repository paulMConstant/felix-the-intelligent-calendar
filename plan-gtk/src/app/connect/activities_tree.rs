//use gtk::prelude::*;

use crate::app::App;

impl App {
    pub fn connect_activities_tree_buttons(&self) {
        //let entities_add_button: gtk::Button = self
        //.builder
        //.get_object("EntitiesAddButton")
        //.expect("Could not load EntitiesAddButton from ui file.");

        //let entities_list_store: gtk::ListStore = self
        //.builder
        //.get_object("EntitiesListStore")
        //.expect("Could not load EntitiesListStore from ui file.");

        //let work_hours_button: gtk::Button = self
        //.builder
        //.get_object("WorkHoursButton")
        //.expect("Could not load WorkHoursButton from ui file.");

        //work_hours_button.connect_clicked(move |_| {
        //entities_list_store.insert_with_values(None, &[0], &[&"Prout"]);
        //});

        //let entities_list_store: gtk::ListStore = self
        //.builder
        //.get_object("EntitiesListStore")
        //.expect("Could not load EntitiesListStore from ui file.");

        //entities_add_button.connect_clicked(move |_| {
        //entities_list_store.insert_with_values(None, &[0], &[&"Prout"]);
        //});
    }
}
